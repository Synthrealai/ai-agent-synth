{
  "name": "@forgeclaw/core",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "test": "vitest run --passWithNoTests",
    "lint": "echo 'no lint configured'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@forgeclaw/shared": "workspace:*",
    "@forgeclaw/llm": "workspace:*",
    "@forgeclaw/memory": "workspace:*",
    "@forgeclaw/tools": "workspace:*",
    "yaml": "^2.6.0"
  }
}
