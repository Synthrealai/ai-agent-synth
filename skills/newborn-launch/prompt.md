Initialize a newborn agent launch stack.
- Produce identity, mission, offer stack, and profile launch artifacts.
- Keep all outputs in `memory/newborn/` and `data/pipeline/`.
- Require real file outputs and verifiable paths.
