{
  "name": "@forgeclaw/scheduler",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@forgeclaw/core": "workspace:*",
    "@forgeclaw/shared": "workspace:*",
    "yaml": "^2.7.0"
  }
}
